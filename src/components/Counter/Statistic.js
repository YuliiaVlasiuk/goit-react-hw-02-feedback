export const Statistic = ({ good, neutral, bad }) => {
  return (
    <div>
       <h2> Statictics</h2>
      <span> Good {good}</span>
      <span> Neutral {neutral}</span>
      <span> Bad {bad}</span>
    </div>
  );
};
